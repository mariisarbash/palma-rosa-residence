
  # Create SVG Logo

  This is a code bundle for Create SVG Logo. The original project is available at https://www.figma.com/design/pGRPxYLM1l07wt9GZz8cTO/Create-SVG-Logo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  